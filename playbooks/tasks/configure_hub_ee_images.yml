---
- name: Collect configurations | hub_ee_image
  ansible.builtin.include_tasks:
    file: "configure_items.yml"
  vars:
    __configure_aap_dir: "{{ item['directory'] }}"
    __configure_aap_setting: "{{ item['name'] }}"
    __configure_var_name: "{{ item['default_attr'] }}"
    __configure_var_env_name: "{{ item['env_attr'] }}"
  loop: "{{ configuration_items }}"
  when:
    - item['name'] == 'hub_ee_images'

- name: INCLUDE_ROLE | infra.aap_configuration.hub_ee_image
  ansible.builtin.include_role:
    name: infra.aap_configuration.hub_ee_image
  when:
    - hub_ee_images is defined
    - hub_ee_images | length > 0
...
